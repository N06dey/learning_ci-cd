build:
  maxIssues: 0

naming:
  PackageNaming:
    active: false # temporarily disable, we’ll fix packages later
  InvalidPackageDeclaration:
    active: false # temporarily disable
  FunctionNaming:
    ignoreAnnotated:
      - Composable # allow PascalCase for @Composable

style:
  MagicNumber:
    ignorePropertyDeclaration: true
    ignoreEnums: true
    ignoreAnnotation: true
    ignoreCompanionObjectPropertyDeclaration: true
  NewLineAtEndOfFile:
    active: false # temporarily disable